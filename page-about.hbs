{{!< default}}

{{!-- The tag above means: insert everything in this file
into the {body} tag of the default.hbs template --}}


{{#post}}
{{!-- Everything inside the #post block pulls data from the page --}}

<main id="site-main" class="site-main">
<article class="article {{post_class}}">

    {{#match @page.show_title_and_feature_image}}
        <header class="article-header gh-canvas">

            <h1 class="article-title">{{title}}</h1>

            {{#if feature_image}}
                <figure class="article-image">
                    {{!-- This is a responsive image, it loads different sizes depending on device
                    https://medium.freecodecamp.org/a-guide-to-responsive-images-with-ready-to-use-templates-c400bd65c433 --}}
                    <img
                        srcset="{{img_url feature_image size="s"}} 300w,
                                {{img_url feature_image size="m"}} 600w,
                                {{img_url feature_image size="l"}} 1000w,
                                {{img_url feature_image size="xl"}} 2000w"
                        sizes="(min-width: 1400px) 1400px, 92vw"
                        src="{{img_url feature_image size="xl"}}"
                        alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
                    />
                    {{#if feature_image_caption}}
                        <figcaption>{{feature_image_caption}}</figcaption>
                    {{/if}}
                </figure>
            {{/if}}

        </header>
    {{/match}}

    <section class="gh-content gh-canvas">
        {{content}}
    </section>

    <div class="post-feed" id="authors">
        {{#get "authors" limit="all" include="count.posts" order="count.posts desc"}}
            {{#foreach authors}}
                <article class="post-card post author-card {{#unless profile_image}} no-image{{/unless}}">
                    <a class="author-card-header" href="{{url}}">
                        <span class="author-card-visual"{{#if cover_image}} style="background-image: url({{img_url cover_image size="m"}})"{{/if}}>
                            {{#if profile_image}}
                                <img class="author-profile-image" src="{{profile_image}}" alt="{{name}}">
                            {{/if}}
                        </span>
                    </a>
                    <div class="post-card-content">
                        <a class="post-card-content-link" href="{{url}}">
                            <header class="post-card-header">
                                <h2 class="post-card-title">{{name}}</h2>
                            </header>
                            <section class="post-card-excerpt">
                                <div class="author-stats">{{plural count.posts empty='No posts' singular='% post' plural='% posts'}}</div>
                                {{#if bio}}
                                    <div class="author-bio">{{bio}}</div>
                                {{/if}}
                            </section>
                        </a>
                    </div>
                </article>
            {{/foreach}}
        {{/get}}
    </div>

</article>
</main>

{{/post}}
